import styled from '@emotion/styled';

const S = {
  Container: styled.div`
    cursor: pointer;
    display: flex;
    align-items: center;
    font-size: 10px;
  `,
  IconWrapper: styled.div`
    margin-left: 15px;
  `,
};

export { S };
