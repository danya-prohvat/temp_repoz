import styled from '@emotion/styled';

const S = {
  A: styled.a`
    display: flex;
    align-items: center;
    cursor: pointer;
    font-size: 22px;
    margin-bottom: 10px;
  `,
};

export { S };
