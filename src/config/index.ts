import { env } from 'config/config';
import { Global } from 'styles/Styles';
import { whiteTheme } from 'styles/themes';
import { useTranslation } from 'react-i18next';

export { env, Global, whiteTheme, useTranslation };
